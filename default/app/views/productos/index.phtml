<?php
View::template("plantilla");
?>


<h1>Productos</h1>
<a href="/Kumbiaphp/productos/registrar" class="btn btn-primary">Registrar Producto</a>
<style>
    .hidden-id { display: none; }
</style>
<table class="table table-striped table-hover" id="myTable">
    <thead>
    <tr>
        <th class="hidden-id">id</th>
        <th>Categoría</th> <!-- Cambiado de categorias_id -->
        <th>Nombre</th>
        <th>Precio</th>
        <th>Stock</th>
    </tr>
    </thead>
    <tbody>
    <?php foreach ($productos as $p): ?>
        <tr>
            <td class="hidden-id"><?= $p->id ?></td>
            <td><?= $p->categoria_nombre ?? 'N/A' ?></td>
            <td>
                <a href="http://localhost/Kumbiaphp/productos/show/<?= $p->id ?>">
                    <?= $p->nombre ?>
                </a>
            </td>
            <td>$<?= number_format($p->precio, 2) ?></td>
            <td><?= $p->stock ?></td>
        </tr>
    <?php endforeach; ?>
    </tbody>
</table>

<!-- DataTables CSS -->
<link rel="stylesheet" href="/DataTables/datatables.css" />

<!-- DataTables JS -->
<script src="/DataTables/datatables.js"></script>

<script>
    // Configuración para habilitar la búsqueda y otras funcionalidades de DataTables
    let table = new DataTable('#myTable', {

    });
</script>

<?= Form::open() ?>

<!-- Botón Regresar -->
<?= Formbs::btn_regresar('Regresar', ['onclick' => 'window.history.back()']) ?>

<?= Form::close() ?>

<!-- Script para limpiar formulario -->
<script>
    function limpiarFormulario() {
        document.querySelector('form').reset();
    }
</script>
